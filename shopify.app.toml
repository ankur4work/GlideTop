# GlideTop — Shopify app configuration
# https://shopify.dev/docs/apps/tools/cli/configuration
#
# `shopify app deploy` pushes this file's URLs to the Partner Dashboard, so it
# is the source of truth for them — edit here, not in the dashboard UI.

client_id = "1b4036bce75ff13b0929fd06e72b792a"
name = "GlideTop"
handle = "glidetop"
application_url = "https://glidetop.onkra.online"
embedded = true

[build]
include_config_on_deploy = true

[access_scopes]
# GlideTop reads the shop's subscription state and writes one app-owned
# metafield recording the tier. Neither needs an Admin API access scope, so this
# stays empty — an app that requests nothing is the easiest kind to approve.
scopes = ""

[auth]
redirect_urls = [
  "https://glidetop.onkra.online/api/auth",
  "https://glidetop.onkra.online/api/auth/callback"
]

[webhooks]
api_version = "2024-10"

  [[webhooks.subscriptions]]
  topics = [ "customers/data_request" ]
  uri = "/api/webhooks"

  [[webhooks.subscriptions]]
  topics = [ "customers/redact" ]
  uri = "/api/webhooks"

  [[webhooks.subscriptions]]
  topics = [ "shop/redact" ]
  uri = "/api/webhooks"

[app_proxy]
url = "https://glidetop.onkra.online/api/glidetop"
subpath = "glidetop"
prefix = "apps"

[pos]
embedded = false
