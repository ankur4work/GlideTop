node_modules
web/node_modules
web/frontend/node_modules
web/frontend/dist

.git
.github
.deploy
.vscode

extensions
*.md

# Must be recursive: a bare ".env" only matches the repo root, which would let
# a developer's web/.env — containing the real API secret — into the image.
**/.env
**/.env.*
!**/.env.example

*.log
